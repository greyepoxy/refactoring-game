{
  "name": "refactoring-game-typescript",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "npm run compile && npm run lint",
    "compile": "tsc -p .",
    "lint": "tslint -p .",
    "lint-fix": "npm run lint -- --fix"
  },
  "devDependencies": {
    "prettier": "^1.16.3",
    "tslint": "^5.12.1",
    "tslint-config-prettier": "^1.17.0",
    "tslint-plugin-prettier": "^2.0.1",
    "typescript": "^3.3.1"
  },
  "prettier": {
    "tabWidth": 2,
    "singleQuote": true
  }
}
